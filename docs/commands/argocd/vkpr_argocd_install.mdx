---
sidebar_position: 1
version: v1.0.20
---

# VKPR argocd install

## Description

Install ArgoCD into cluster. For more information about ArgoCD, click [here](https://argo-cd.readthedocs.io/en/stable/).

## Commands

Interactive inputs:

```bash
  vkpr argocd install [flags]
```

Non-interactive without setting values or using `VKPR Values`:

```bash
  vkpr argocd install --default
```

### Parameters

```txt
  --default           Set all values with default
  --domain            Define the domain used by the ArgoCD   Default: "localhost"
  --secure            Specifies if the application will have HTTPS   Default: "false"
  --admin_password    Define Super Admin ArgoCD password   Default: "vkpr123"
  --HA                Specifies if the application will have High Availability   Default: "false"
```

### Values File Parameters

```jsx title="vkpr.yaml"
global:
  domain:   <String>
  secure:   <Boolean>
  argocd:
    ingressClassName:   <String>
    HA:   <Boolean>
    adminPassword:    <String>
    metrics:    <Boolean>
    addons:
      applicationset:   <Boolean>
    helmArgs:   <Object>
```

---

:::info About the Formula
`Chart Version`: 3.29.4 <br/>
`Application Version`: v2.2.1 <br/>
`Helm Chart`: [argo/argo-cd](https://artifacthub.io/packages/helm/argo/argo-cd) <br/>
:::

### See Also

- [VKPR argocd remove](/docs/commands/argocd/vkpr_argocd_remove) - Uninstall ArgoCD from the cluster.
- [VKPR argocd set repository](/docs/commands/argocd/set/vkpr_argocd_set_repository) - Sync a repository with the ArgoCD.
- [VKPR argocd set applicationset](/docs/commands/argocd/set/vkpr_argocd_set_applicationset) - Apply an Applicationset manifest in the cluster.
