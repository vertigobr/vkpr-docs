---
sidebar_position: 3
sidebar_label: VKPR aws rds destroy
title: VKPR aws rds destroy
---

# VKPR aws rds destroy

## Description

Destroy a RDS database.

## Commands

Interactive inputs:

```bash
  vkpr aws rds destroy [flags]
```

Non-interactive without setting values or using `VKPR Values`:

```bash
  vkpr aws rds destroy --default
```

## Argument Reference

### Flags

```sh
  --default         Use to automatically fill inputs with default value provided on config.json
  --instance_name   Specifies the RDS instance to be destroyed.
```

## Setting Provider credentials

**CAUTION: Setting credentials manually will override the credentials already saved in memory**

### AWS

```bash
  rit set credential --provider="aws" --fields="accesskeyid,secretaccesskey,region" --values="your-accesskey,your-secretaccess,your-region"
```

---

## See Also

- [VKPR aws rds destroy](/docs/commands/aws/rds/destroy) - Execute the pipeline to destroy the rds Cluster.
